{
  "name": "flip-blocks",
  "version": "1.0.0",
  "private": true,
  "description": "Flip Blocks browser game and LAN match server",
  "scripts": {
    "start": "node server/index.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "ws": "8.21.3"
  }
}
